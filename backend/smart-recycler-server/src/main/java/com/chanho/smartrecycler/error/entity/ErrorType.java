package com.chanho.smartrecycler.error.entity;

public enum ErrorType {
    CAMERA_ERROR,
    INFERENCE_TIMEOUT,
    LOW_CONFIDENCE,
    BIN_FULL,
    SERVER_SEND_FAILED,
    SORTING_FAILED,
    UNKNOWN_ERROR
}
