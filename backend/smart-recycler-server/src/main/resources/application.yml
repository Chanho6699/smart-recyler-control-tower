server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3308/smart_recycler?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: recycler
    password: recycler
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
